import {createTheme} from "@mui/material";

const theme = createTheme ({
    palette: {
        primary: {
            light: "#e254ff",
            main: "#aa00ff",
            dark: "#7200ca",
            contrasText: "#7200ca"
        }
    }
})

export default theme;